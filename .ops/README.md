# Ops Folder

This folder contains non-deploy operational files:

- `reports/` validation and cleanup reports
- `runtime/` temporary logs/output
- `tools/` local helper scripts
- `notes/` working notes/TODOs
